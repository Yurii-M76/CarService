type TNavigationItem = { path: string; label: string; accent: boolean };

export type TNavigarionItems = TNavigationItem[];

export type THeaderValues = {
  phone: string;
  workSchedule: string;
  title: string;
  accentButtonLabel: string;
  secondaryButtonLabel: string;
};
